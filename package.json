{
  "name": "promptscore",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @promptscore/web dev",
    "build": "pnpm --filter @promptscore/web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.22.0",
  "devDependencies": {
    "prettier": "^3.8.3"
  }
}
